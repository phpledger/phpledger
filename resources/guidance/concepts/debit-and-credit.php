<?php
declare(strict_types=1);

/**
 * Interface copy for the help bubble. English source strings for pl_t(); see this directory's
 * README for the shape. 'review' stays 'placeholder' until the guidance review (docs/accounting/
 * guidance/CONCEPTS.md) has passed the wording.
 */
return [
    'title' => 'Debit and credit',
    'explanation' => 'Every entry has two sides that must be equal. A debit increases what the business owns or what it has spent; a credit increases what it owes, what its owners have put in, or what it has earned. Nothing is good or bad about either word: they are only the two columns a balanced entry fills.',
    'here' => 'On this report every account sits in the column its balance falls on, and the two column totals must agree.',
    'document' => ['label' => 'Open the guide', 'href' => '/help'],
    'review' => 'placeholder',
];
