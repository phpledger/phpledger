# Shared Claude and Codex state

## Latest checkpoint, 21 September 2026 (Claude): the fixed-asset register (M14) is built, not merged

- **Branch `m14/fixed-assets`, rebased onto `master` `3fc315d`, one commit, not pushed, not merged.** Issue **#95**. Migration **046_fixed_assets**: it started at 042, and the plugin runtime, the sample skeletons and the ownership register each took a number while it was being built, so it has been renumbered twice. It appears in five places — the file name, the installer label in `install_check_functions.php`, two lines in `tools/package-files.json`, the `migrations` list in `resources/modules/fixed-assets.json`, and prose in the docs. Its `pl_core_audit` `entity_type` MODIFY carries the union of 041's list plus `asset` and `asset_class`; 044 uses an audit table of its own and adds nothing to that ENUM.
- **The register is a subsidiary ledger, not a second set of books.** Nothing in it writes a journal row: an acquisition, a depreciation run and a disposal all go through `pl_post_journal()`. Every figure it reports is a sum of `pl_asset_events` rows and every event row is backed by one journal, so `pl_asset_register()` compares the register with the trial balance of each control account and **states the difference as a figure** rather than asserting there is none. A test posts a manual journal straight into the cost account and asserts the report names the account and the 500.0000, because that is the case the reconciliation exists for.
- **Corrections are linked reversals, enforced by the database.** `pl_asset_events` has triggers refusing UPDATE and DELETE. A correction is a mirror row with every amount negated, carrying **the period of the row it reverses**, so re-running a corrected period computes the difference and never a duplicate.
- **The three owner decisions of 21 September are in the module, not only in the data.** (1) The gain or loss on disposal goes to one **non-operating income** account carrying both signs; routing it through operating expenses distorts gross margin and operating profit. (2) **Whole months**: a full month in the month an asset enters service whatever day it arrived, and none in the month it leaves. The earlier day-weighted schedule is gone and the `convention` column went with it. "None in the month of disposal" is not "none in the period of disposal" — an asset sold in June of an annual book still owes January to May, and `pl_dispose_asset()` posts exactly that, dated on the day, **before** removing the asset, so a gain or loss can never absorb missing depreciation. (3) Acquisitions and disposals post through the module.
- **`/assets` was never reachable and the route sweep caught it.** `www/phpledger/public/assets/` is the static mount — `app.css`, `app.js`, the icons, the fonts, the brand images — so the web server answered `/assets` and `index.php` never ran. The screens are now `/fixed-assets`, `/fixed-assets/detail` and `/fixed-assets/depreciation`; the report keeps `/reports/asset-register`. A new assertion refuses **any** manifest route whose first segment is a real directory under the document root, so the next module cannot repeat it.
- **The PPE heading is found by `semantic_key`, never by name or number.** `pl_asset_cost_group_key()` is the one named place and returns `core.group.asset.ppe`; the allocator reads the group number off whatever account carries that key, so the module never knows the number. No `1-200` literal exists in code or tests: the test builds the heading at group 250 to prove the mechanism rather than a number, and a second fixture with no key proves the fallback still opens the next free group and that the module writes no semantic key of its own.
- **Migration `046_fixed_assets`, renumbered three times** — 042, then 045, then 046, as the plugin runtime, the sample skeletons, the ownership register and the chart headings each landed first under B62. Check it again before merging.
- **The chart headings changed this branch's tests, not its code.** `core.group.asset.ppe` is a real account on `1-200-00000-00` in `core-starter-1.2.0.json` and `pl_create_company()` inserts the nineteen headings, so the fixture that used to build its own heading with that key would now collide on `uq_account_semantic` — the constraint that forced the `core.group.*` namespace — and the fixture that proved the no-key fallback was no longer keyless. The test now reads the group number out of the chart the way the module does, asserts the heading is unpostable and carries **no role** (`pl_ar_control()` and `pl_advance_control()` each need exactly one active account per role, so a heading with one would refuse every document), and reaches the fallback by clearing the key, which is what a hand-built or pre-headings book actually looks like.
- **The demo packs merged cleanly and were regenerated anyway.** A bad resolution in generated data merges without a conflict and is silently wrong, so they were rebuilt from `tools/build-demo-packs.py` and compared: byte-identical, so the merge was right — now known rather than assumed.
- **`tests/package-builder-test.py` is not in `composer check`.** It is Python, and it is the only gate that checks `tools/package-files.json`, including the rule that `resources/distribution/` never reaches the release allowlist. Run it by hand after touching the allowlist: `Ran 13 tests ... OK`.
- **Evidence:** own Compose project `plfixedassets`, own subnet `10.221.7.0/24`, MySQL 8.4, timeout inside the container. `PHP lint: 335 files, 0 failures.`, PHPStan level 5 `[OK] No errors`, sample data valid, i18n backlog 12 against a ceiling of 12, **`547 tests, 0 failures.`** Not run: MariaDB, a fresh install from the built ZIP, and a `tools/verify-upgrade.php` baseline.

## Latest checkpoint, 21 September 2026 (Claude): 1.2.1 M9 built, green, not merged

- **Branch `m9/pos-and-distribution`, rebased onto `master` `843d941` (after m15 and m14 landed), one commit, not pushed, not merged.** Owner decisions **B34**, **B35**, **B36**, **B58** and the taken research decisions in `docs/design/1.2-2026-09/distribution-research/DECISIONS.md`.
- **No migration at all.** A counter sale is an invoice, a van sale is an invoice, the credit limit is the column 014 already carries and the cost split is a read. Migrations end at **046** and this branch adds none, which is why it could go last without renumbering anything.
- **On merged master `843d941`: `composer check` 560 tests, 0 failures**, lint 344 files clean, PHPStan `[OK] No errors`, sample data valid, i18n backlog **12 against the ceiling of 12** (all pre-existing, none mine), i18n sweep 51 of 55 routes. Separately **`python tests/package-builder-test.py` — `Ran 13 tests ... OK`**, which is the only gate on `tools/package-files.json` and is not in `composer check`. Own Compose project `plcountertill`, own subnet `10.218.44.0/24` checked free first, timeout inside the container.
- **m15's chart headings broke nothing here, and that was checked rather than assumed.** No fixture in this branch builds an account, a heading or a chart by hand: both test files compose `inventory_fixture()` and `trading_fixture()`, so the `uq_account_semantic` collision that caught m14 has no surface. The only hand-built accounts are `1300` and `2100` in `tools/simulate-distribution.php`, the same codes `inventory_fixture()` uses, with no semantic key. The simulation was re-run after the rebase and still reconciles to the control account under the new nineteen-heading chart.
- **The till is not a parallel path.** `pl_post_counter_sale()` goes through `pl_save_and_post_ar_document()`. No counter-sale table, journal or number series exists and none should be added: a counter sale **is** an invoice that names a warehouse and took cash.
- **The one thing that will catch the next person: every document line needs an account.** `pl_ar_posting_plan()` refuses a line without one, and a cashier must never be asked to choose a general-ledger account. `pl_counter_sale_line_account()` takes the product's own `sales_account_id` and refuses by name a product that has none, rather than defaulting into a misclassification of revenue. The credit note in the scenario takes the income account of the line it credits.
- **The van settlement now carries money and credit.** `pl_van_day_sales()` and `pl_van_credit_check()`; an approval is refused while a customer is outside their recorded limit, and refused again as stale until the day is reviewed afresh. A NULL limit is not enforced; a recorded zero is.
- **M7's open item is closed:** the settlement's cost totals are under B58, by splitting `pl_van_day_figures()` (unmasked, what a review stores and an approval compares) from `pl_van_day()` (the read, masked). An approver without `cost.view` can still approve; a reader without it sees cost nowhere, including through the API.
- **Deviation, accepted as a decision:** the till is `/counter` on the `ar` manifest, not a warehouse-aware `/pos/checkout`. `/pos` keeps the `pos-showcase` sample cart and is relabelled "Point of sale (sample)". Reversible; recorded in the 1.2 release plan.
- **For whoever owns the samples:** the distributor's van day sells with a bare `pl_inventory_issue()`, so the settlement's money side is correctly empty. Details in the release plan's M9 section. `resources/demo-packs/` and `demo_pack_functions.php` were not touched.

## Latest checkpoint, 21 September 2026 (Claude): the ownership register (M8a) is built, not merged

- **Branch `m8a/ownership-register`, rebased onto `master` `10546dd`, one commit, not pushed, not merged.** Issue **#92**, owner decisions **B63**, **B64**, **B72 as narrowed by B74**, **B76** and **B58**. Migration **044_ownership_register**: 042 was free when this branch started, the plugin runtime took it and the sample skeletons took 043 while it was being built, so the rebase renumbered it. It appears in exactly five places — the file name, the installer label in `install_check_functions.php`, one line in `tools/package-files.json`, the `migrations` list in `resources/modules/core.json`, and prose in the docs.
- **What it holds.** The B64 company profile gains the legal form, registration number and authority, incorporation date and financial year end, and the letterhead prints them because several jurisdictions require a registration number on a business letter. Two registers — members, effective dated, and directors and officers with the significant-control flag and the nature of control — over their own person records. Share classes and an append-only share ledger with two triggers that refuse an UPDATE and a DELETE. Reports: the ownership snapshot, book value per share, the partner capital account statement in both the fixed and the fluctuating presentation, the related-party transaction report and the director loan movement report. An Open Cap Format export with a reader that proves the round trip. New screens `/ownership`, `/reports/ownership` and `/ownership/export`, all three in the `pl_render()` allowlist, the router, the core manifest's routes and the pseudo-locale sweep.
- **The related-party marker is B72 as corrected by B74, and the correction is the point.** Every person defaults to **not** related. No role, appointment, employment link or significant-control flag creates, implies or back-fills a marker; the flag is a separate attribute with its own two capabilities and its own effective dates; and `relationship` carries only the three kinds IAS 24 names. A test appoints a director who is also a supplier, changes nothing else, and asserts the related-party report is **empty** — so if anybody ever makes a role imply a marker, that test fails. A duplicate-attribute cross-check (B74, B75) surfaces candidates for a human to decide and marks nobody.
- **Two deliberate deviations from the release plan's M8a paragraph, both recorded in the plan.** (1) The registers do **not** use "the same party records" as receivables and payables: `pl_parties` carries `ck_party_roles`, which requires a customer or a vendor, and a shareholder who never trades is neither. They share their own person table and the related-party marker is the only link, which is what B72 designed it to be and what B76 rule 2 requires — recorded, never inferred. (2) A share class stores **no issued count**; it is the ledger summed to a date, because a stored count is the second source of ownership B63 forbids and it is the field that goes stale first.
- **Migration 044 does not touch the `pl_core_audit` `entity_type` ENUM at all.** The register's audit is its own company-scoped immutable table, the way 041 did it for `pl_user_audit`, because `pl_core_audit.book_id` is `NOT NULL` and nothing in this register belongs to a book. The 037/038 union collision therefore cannot repeat here.
- **The hook seam now sits on the real runtime.** M8 merged while this was being built, and checking the seam against it found a genuine defect: every register write emits from inside `pl_ledger_transaction()`, and `pl_hook_assert_unlocked()` rightly refuses to run an action while a book row is locked. `pl_ownership_emit()` therefore hands the payload to `pl_hook_after_commit()`, so a package's callback runs after the outermost commit with every lock released, and never at all on a rollback. A package registers with `pl_add_action('ownership.share_event.recorded', …)`; nothing needs bridging. The six points are deliberately **not** added to `pl_hook_points()`, because that list is the published plugin surface with a recorded snapshot owned by M8 and declaring a new surface belongs with that milestone's contract.
- **`resources/modules/core.json` changed** (`capabilities`, `migrations`, `routes`, `reports`, `api_operations`, `mcp_operations`, `history`), so the core manifest digest moves. Safe for `core` only, for the reason M7 recorded: no per-company `manifest_hash` is stored for a required module. No optional manifest was touched.
- **Three new capabilities**, all owner-only by default: `ownership.manage`, and `relatedparty.view` / `relatedparty.manage` split on purpose — reading a marker is the sensitive fact B58 keeps from whoever manages customers, and the person who prepares the disclosure is usually not the person who decides who is key management personnel, so the read is grantable without the write. A test builds exactly that custom role.
- **Still open, and named in the accounting reference:** PHP Ledger records no per-party allowance for doubtful debts, so the IAS 24.18(d) provision is reported as `null` with the chart's contra-asset accounts listed instead; the members register's effective-dated profit share and `pl_owner_partners.profit_share` are not reconciled, so editing the ratio on the Owner screen can leave them disagreeing; liquidation preferences are not modelled, so book value per share overstates an ordinary share where a preference class is in issue. Five questions go to the accountant in [OWNERSHIP-REGISTER.md](docs/accounting/OWNERSHIP-REGISTER.md) §10, which also carries the two-period worked examples for a private company and a partnership.

## Latest checkpoint, 21 September 2026 (Claude): 1.3 M15, the chart of accounts reads as accounting

- **Branch `m15/chart-headings` from `master` `57b4d49`, three commits, not pushed, not merged, nothing deployed.** Every report of every book read **"Group 1-100"**: `pl_report_tree_node()` names a group from an account row whose code is a heading code, and **no chart package contained one**. Migration 036 renumbered existing charts into groups and named none of them either. Seed data and a migration, not schema — there is no group-name table and none was added.
- **`resources/coa/core-starter-1.2.0.json`** carries the 1.1.0 chart's thirteen postable accounts byte-identical plus **nineteen named headings** (the owner's fourteen groups and the five classes). `core-starter-1.1.0.json` and `1.0.0` keep their bytes and still ship: a book records the digest of the chart it was installed from, so a version already in someone's `pl_template_installations` is never rewritten in place. A package now has two lists — `accounts` (postable, carry the starter purposes the prior-foundation review maps) and `headings` (names) — and keeping them apart is what stops nineteen headings appearing among the thirteen purposes the owner is asked to map.
- **Every heading carries its own `semantic_key` in the `core.class.*` / `core.group.<class>.*` namespace**, which is how a module finds a group — never the name, which is translated and editable, and never the number, which 036 allocated per chart. The namespace is separate because `uq_account_semantic (book_id, semantic_key)` is one account per key per book, so `core.group.equity.drawings` and the leaf `core.equity.drawings` coexist and a lookup meant to find somewhere to post can never land on a heading. **`core.group.asset.ppe` is the fixed-asset key `m14/fixed-assets` should hold.** `pl_account_heading_by_key()` is the one resolver: it matches the key, falls back to the bundled chart's code for a chart that predates the keys, says which of the two answered, and returns null for a book with no such group so the caller asks its owner rather than stopping. **No heading carries a `role`** — `pl_ar_control()` and `pl_advance_control()` need exactly one active account per role and would refuse every document otherwise.
- **Migration `045_chart_headings`** (042, 043 on `master`; 044 is the unmerged `m14/fixed-assets`). Nineteen statements, every one an `INSERT INTO pl_accounts`: no UPDATE, no DELETE, no posting account touched, no schema change, `pl_core_audit` untouched. Classes from the class digit; a group from the `semantic_key` of the single starter purpose in it, and **refused where two purposes share a group**, which is left for its owner to name. Both a code and a key `NOT EXISTS` on every statement, so an upgrade cannot die on `uq_account_semantic`. `1-200` only where the numbering provably means it (no conversion receipt, cash still at `1-100-10001-00`). Reversal: `DELETE FROM pl_accounts WHERE creation_key LIKE 'migration-045-%'`.
- **An empty group is deliberately off a report and on the chart screen**, and nineteen help concepts (40–70 plain words each, all `placeholder`) sit under every heading in the report tree and beside it on the chart screen. Each carries an allowlisted `https://phpledger.com/learn/<slug>/` link, rendered `rel="noopener noreferrer"`, pointing at one of the three **live** articles that covers it — `chart-of-accounts`, `profit-and-loss-vs-balance-sheet` or `cash-vs-accrual`. **No slug is invented for an article yet to be written**, and the test reads the live slugs out of `www/website/src/pages/` so a dead link cannot ship.
- **No chart heading may carry a `role`, and this would have shipped as a mysterious posting failure.** `pl_ar_control()` (`ar_ap_functions.php:433`) and `pl_advance_control()` (`advance_functions.php:27`) each refuse unless **exactly one** active account holds the role — "there must be one unambiguous default" — so a heading holding `receivables`, `payables` or an advances role would make the book refuse every customer and supplier document and every advance. `pl_save_account()` already refuses a role on a heading code; the chart loader and a test now hold the same line.
- **Four things fixed in passing that this change would otherwise have broken:** every demo-pack checkpoint reconciliation (`pl_demo_pack_reconcile()` compares the whole trial balance with `!==`), the three report CSV exports, the account pickers on four screens (`pl_company_context()` now carries `is_postable`), and both `tools/verify-upgrade.php` baselines — **the verifier was edited but not run**; it is not in `composer check`.
- **Evidence, after rebasing onto `master` `bb739aa`:** own Compose project `plchartnames`, own subnet `10.215.15.0/24`, MySQL 8.4, `COMPOSER_PROCESS_TIMEOUT` inside the container, torn down with `down -v`. Exit 0: lint 327 files, PHPStan clean, i18n sweep 47 of 50 routes, **i18n backlog 12 against the ceiling of 12** with all nineteen concepts in (a concept file is not a template and adds nothing to that count), **531 tests, 0 failures** — 522 on master plus the nine added here, including the two regression guards: no report node of a new book or of a converted one falls back to the placeholder. The corrections to the two earlier m15 entries — the live article links, `044` being the ownership register rather than m14's, and `views/packages.php` already fixed on master — are the last entry in [AGENT_MESSAGES.MD](AGENT_MESSAGES.MD).

## Latest checkpoint, 21 September 2026 (Claude): 1.2.1 M10, the onboarding redesign and the skeleton import

- **Branch `m10/onboarding-redesign`, rebased onto `master` `eb6295d`, committed, not pushed.** Decision **B50**, scoped in 1.2 and never built. The owner's report was that "the business setup does not allow to choose chart of account and other structure from a sample company". It does now.
- **A sample has two halves and only one of them is structure.** `sample_structure_functions.php` reads a declared `structure` section - chart, modules, packages, numbering, policies, document footer wording, customers, suppliers, products, tax codes - and touches `events`, `drafts`, `checkpoints` and `monthly_support` nowhere. A structure document that carries any of them is refused, not partly imported. `resources/demo-packs/` and `tools/build-demo-packs.py` were **not** touched: the section the packs must eventually carry is specified in the matching [AGENT_MESSAGES.MD](AGENT_MESSAGES.MD) entry, and until they carry it the same data ships in `resources/sample-structures/`, generated by `tools/build-sample-structures.py` from the packs' own structure keys and pinned to each pack's digest.
- **A skeleton posts nothing, and that is checked against the book rather than asserted.** Every write goes through the ordinary service for that thing, and before the transaction commits the importer proves there is no journal, no journal line, no document, no draft, and a trial balance that is empty and balanced. It creates a **real business**, not an isolated sample; a **full** sample still carries fictional history and is still only ever a separate sample company.
- **The wizard is five named stages** - Start, Business, Source, Review, Ready - in the installer's own Workbench language, replacing six linear steps. Source is the new one. `tests/onboarding_skeleton_test.php` walks all five over HTTP and creates a business through them, and `/onboarding` and `/sample-chooser` are now in the i18n route sweep, which had never requested either.
- **Migration `043_sample_skeletons`** (042 is `m8/plugin-runtime`; renumber at merge per B62). `resources/modules/core.json` changed, so the core manifest digest moves - safe for `core` only. Automatic package activation is one named seam that calls the plugin runtime when it exists and records `runtime_unavailable` when it does not, so nothing depends on unmerged work.
- **Found in passing:** `pl_render()` extracts with `EXTR_SKIP` while holding `$view`, so a data key named `view` is dropped without a word and the screen reads a string where it expects an array. Also: the distribution sample's products name semantic keys its own vertical profile does not declare, and two other samples' products name none.
- **Evidence:** `composer check` in its own Compose project `phpledger-m10` on its own subnet `10.237.10.0/24`, MySQL 8.4, torn down with `down -v`. Exit 0: lint 311 files, PHPStan clean, **469 tests, 0 failures** (461 at the branch point plus the eight added here). The i18n ceiling M11 had just lowered to 12 is unchanged and the redesigned wizard contributes none of it. A `master` baseline was run separately to establish that the one numbering failure seen mid-session was already on `master` at the branch point and had been fixed there by `f9f42ba`.

## Latest checkpoint, 21 September 2026 (Claude): the samples are a 1.2.0 product, and the demo can stand up on them

- **Built on `m13/sample-data-refresh` from `master` `cdd9dba` (tag `v1.2.0`); three commits, not pushed, not merged, nothing deployed.** No migration was added, so `042` is still the next free number, and no new shipped file, so `tools/package-files.json` is unchanged.
- **Every sample now shows 1.2.0.** The company profile is filled in everywhere, numbering is set per document type before anything posts, the reserved contra groups are actually posted to, and capital, an owner loan, its repayment and drawings are in every pack (B61) with Cedar Studio as the partnership. Four packs between them cover both discount policies and both price modes with a full trading invoice; Harbor Supply keeps the driver day and the second person who approves its settlement; Harbour Trade keeps the six advance and refund cases. Everything posts through the same service a screen calls, after the pinned contract has reconciled, and refuses to finish unless the reports still balance.
- **1.3 has history waiting for it.** Every pack carries `anticipated_1_3` for issues #93 to #98 — deferred income, an asset disposed mid-life, a loan instalment schedule, a closeable 2025 with its closing journal and partner appropriation, and a payroll month as totals per element. Each block says in its own words that no screen produces it.
- **Four defects found on the way, three of which would have blocked the demo cutover**: the pack generator had drifted from its own output and dropped the two advances controls from every checkpoint; the seeder kept a stale copy of the demo document ceiling that refused an 85-source pack; `pl_save_role()` could not create a role under the demo's INSERT-only grant; and the hourly demo reset would have refused for ever once a sample seeded a second person. All four are fixed here and every fix is proved.
- **Evidence:** `composer check` — `458 tests, 0 failures.`, PHPStan clean, sample-data validator unchanged at `{"packs":7,"events":77,"documents":42,"items":16}` with its eight rejections; the demo smoke under restricted grants — eleven packs, 396 checkpoints, 100 practice records left in every one; and the reset guard proved to refuse two kinds of stranger and to accept the real seeded state. Details in [AGENT_MESSAGES.MD](AGENT_MESSAGES.MD).
- **Worth knowing:** `python tools/build-demo-packs.py --check` is not part of `composer check`, which is how the generator drift went unnoticed. Run it before touching a pack.

## Latest checkpoint, 21 September 2026 (Claude): the upgrade verifier is a usable release gate again

- **All eight `tools/verify-upgrade.php` baselines pass**, on branch `claude/eloquent-chebyshev-1b0885` from `master` `3ad36fa`; one commit, not pushed, not merged. The six recorded as broken in `docs/DEVELOPMENT.md` by the owner-accounts session (`0c2f551`) were fixture problems throughout: a baseline called a helper newer than the schema it simulates, or asserted that nothing moved where a migration legitimately moves something. **No baseline was weakened or removed, no assertion was dropped, and no migration was touched.** Until now a real upgrade regression in any of those paths was invisible behind the noise.
- **What changed.** The actor is seeded instead of created through `pl_create_user()` (`032_user_names` adds `pl_users.username`). `preview-0.5.0` and `stable-1.1.1` seed their company and the bundled **1.0.0** chart directly instead of calling `pl_create_company()`, so migration 036 actually converts a prior chart in them; `stable-1.1.1` seeds its whole 1.1.1 stock state, because `inventory-locations` is now manifest 1.1.0 and declares `038_stock_documents` and every warehouse read names the columns 038 added, so today's stock services correctly refuse a 1.1.1 schema. Two stale assertions were made exact rather than blanket: `039` legitimately provisions the two advances controls in an existing book, and `036` legitimately renumbers its chart, and the baselines now state both outcomes by name.
- **`stable-1.1.1` proves more than before:** after the upgrade a locations write is refused until the owner reviews the version the upgrade brought, and only after that review does the 1.1.1 stock read back through the current services at exact figures — six units worth 60.0000 in the default warehouse, four worth 40.0000 in the van, 100.0000 valued and reconciled to the ledger.
- **Evidence:** own Compose project `phpledger-verifyup`, own subnet `10.231.44.0/24`, MySQL 8.4. `composer check` exit 0 — lint 291 files, PHPStan clean, **438 tests, 0 failures**; the suite was not re-run on `master` for comparison, and no application file was touched. All eight baselines run verbatim, and two deliberate mutations were confirmed to fail the right assertions before being reverted.
- **Still open:** the verifier runs in no CI job, so nothing stops these fixtures rotting again; a real 1.0.0-shaped book still cannot complete today's prior-foundation review, which demands thirteen mappings against its six accounts, and no baseline covers that; there are no `stable-1.1.0`/`1.1.2`/`1.1.3` baselines. Details and the exact output are in the matching [AGENT_MESSAGES.MD](AGENT_MESSAGES.MD) entry.
## Latest checkpoint, 21 September 2026 (Claude): 1.2 M7 built, not merged

- **Branch `m7/users-module` from `4c223a7`, committed, not pushed.** The full Users module (B44, B58, B17, B53, B12): migration `040_users_and_roles.php`, a capability catalogue, `pl_user_can()` replacing about fourteen literal owner-only checks, the three protected system roles as installation-wide definitions assigned per company, custom roles per company, invitations, forced password resets, server-side revocable sessions, a profile page, per-report cost visibility, an immutable user audit, and the first MeekroORM records (`PL_Model`, `PL_User`, `PL_Role`, `PL_Capability`).
- **Authorisation has one question now:** `pl_user_can($actor, $company, $capability)`. `pl_require_company_access()` is unchanged and still the membership boundary and read/write gate; it runs first at every converted call site. `tests/capability_equivalence_test.php` proves the conversion changed no outcome for owner, accountant, viewer, outsider and inactive actors across every gate converted, as a tri-state (allowed / denied / no-access).
- **The 1.1 role ENUM on `pl_company_members` is mirrored, not replaced.** Every membership write sets both it and `role_id`; a custom role mirrors to `accountant` or `viewer` by whether it can write, and never to `owner`. 1.3 drops the ENUM.
- **`resources/modules/core.json` changed**, so the core manifest digest moves. Safe for `core` only: no per-company `manifest_hash` is stored for a required module. No optional manifest was touched.
- **Templates are under PHPStan** (`www/phpledger/templates` in `paths`, one scoped ignore for the variables `pl_render()` extracts). Four real findings in existing templates were fixed.
- **Still open after M7:** no mail transport, so invitation, reset and email-confirmation links are shown once on screen; the van settlement's cost totals are not yet under `cost.view` (they feed the approval comparison); module action permissions still name roles until M8's contract 2; `installation.admin` has no consumer until M8's Packages.
- **Migration numbering:** 040 was the next free number at the branch point; renumber at merge in landing order per B62. It appears in four places only — the file name, the installer label in `install_check_functions.php`, two lines in `tools/package-files.json`, and the `migrations` list in `resources/modules/core.json`.


## Latest checkpoint, 20 September 2026 night (Claude): 1.1.3 published, 1.2 started

- **Release [v1.1.3](https://github.com/phpledger/phpledger/releases/tag/v1.1.3) published 2026-09-20 20:40:01 UTC** from `521dbc3` (tag `v1.1.3`). `phpledger-1.1.3.zip` 2,952,255 bytes, SHA-256 `208c0a089ee32d377116026a4d0401790d21706399a5c63b5f5f818a4d3b5482`, 1,502 manifest files, two local builds byte-identical; `phpledger-1.1.3.update.json` 312,687 bytes, SHA-256 `c285f3aa11dc9db697c4ae62ca5750e8103f360f4a1ab19a062a3ae885cb2002`, owner-signed via `.cache/release-1.1.3/sign-1.1.3.py`, verified from 1.1.0, 1.1.1 and 1.1.2; no media kit (B29). All assets re-downloaded anonymously and hash-verified. `composer check` 320 tests, 0 failures; CI green on the tag.
- **It fixes issue #90** (the updater's migrate phase redeclared `pl_database_platform()`): `install_functions.php` and `update_database_functions.php` keep whichever copy of the platform helpers is already loaded, `database_platform_functions.php` documents a one-release compatibility rule, and `tests/update-migrate-test.php` runs the real migrate and verify phases under the copied runtime (fails on 1.1.2's code, passes on 1.1.3's; registered in both CI jobs).
- **Upgrade paths proven with the published archive and the published signed metadata** on disposable MySQL 8.4.9 schemas: from **1.1.2** and from **1.1.1** the update completes (`backup > apply > migrate > verify > runtime > complete`; from 1.1.1 it also applies 034); from **1.1.0** it stops at migrate and restores itself, losing nothing. That last one is **[issue #91](https://github.com/phpledger/phpledger/issues/91)** (open, milestone 1.2): the installed release's helpers are kept for the whole operation and the 1.1.0 copy predates `pl_database_require_trigger_support()`. Every surface states the proven paths and sends 1.1.0 to the manual procedure.
- **Website live at `website-1-1-3-20260920-205507`** (20:56:37 UTC) from `63bcb39`; backup `/var/www/phpledger/data/backups/website-1-1-3-20260920-205507`; 308 files; document root only; demo untouched. Two earlier attempts are part of the record: `website-1-1-3-20260920-204637` was extracted but never switched (the publisher's existing-directory guard stopped a re-run; its directory is unused on the host), and `website-1-1-3-20260920-204912` switched and rolled itself back when a wrong page check in `.cache/publish-website-1.1.3.py` failed. The wrapper's check is corrected. **Wiki** `e822ef4`; **About** names 1.1.3; the v1.1.2 release page points to the fix; #90 closed.
- **Demo unchanged** at `core-1.1.0-af0f4895f85a`, stated on every 1.1.3 surface.
- **1.2 started.** Merged on `master`: MariaDB 10.4 in the CI matrix with the floor proven (full battery green on 10.4.34, decision B31) and member-digest equality in `tools/build-release.py --compare` (B32, issue #75); the stock-locations release surfaces, the bundled-module definition of done, a generic module-matrix test, the compatibility matrix and a `stable-1.1.1` verifier baseline; the re-estimated [1.2 plan](docs/strategy/RELEASE-PLAN-1.2.md) (63 to 120 engineering days, 1.2.0 targeted Friday 19 February 2027, range 22 January to 26 March). Decisions **B31 to B61** record the widened scope; design frames and the owner's Awan distributor prototype are under `docs/design/1.2-2026-09/`.
- Next: 1.2 M2 (numbering and print pipeline, i18n groundwork). Pilot #1 starts on **1.1.3** on 1 October (findings register `docs/repository/PILOT-FINDINGS-2026-10.md`). Open irreversible questions for the owner are listed at the end of the 1.2 plan.

## Latest checkpoint, 20 September 2026 night (Claude): 1.1.2 published, website and wiki live, 1.2 plan approved (decisions 1 to 4)

- **Release [v1.1.2](https://github.com/phpledger/phpledger/releases/tag/v1.1.2) published 2026-09-20 18:43:56 UTC** from `3df5755` (tag `v1.1.2`, on `master`). Assets: `phpledger-1.1.2.zip` 2,951,475 bytes, SHA-256 `24d499d4b3ba249a7b25eb7874fa18c0c0ece778203d32b927bef3cf260eb882` (1,502 manifest files; two local builds byte-identical; CI archive member-identical, bytes differ as before); `phpledger-1.1.2.update.json` 312,687 bytes, SHA-256 `5868d8124d06a2c9e8fafac2a7c7a6e93d984db4ffba97b8f472cc575876b49d`, signed by the owner with the official key and verified as an upgrade from 1.1.0 and 1.1.1; no media kit (B29). All three re-downloaded anonymously and hash-verified. CI on `3df5755`: Foundation checks (two runs) and Release build all succeeded. `composer check` 319 tests, 0 failures.
- **Website live at `website-1-1-2-20260920-191854`** (2026-09-20 19:20:29 UTC, from `00a6448`, carrying the issue #90 notice; backup `/var/www/phpledger/data/backups/website-1-1-2-20260920-191854`; wiki `71e8c82` carries it too). Its predecessor `website-1-1-2-20260920-185427` (18:56 UTC) from `ac0639d` through `.cache/publish-website-1.1.2.py`; backup `/var/www/phpledger/data/backups/website-1-1-2-20260920-185427`; 305 files verified; only the document root changed; `/releases/index.json` offers stable 1.1.2 with `update_json`, and `/releases/1.1.2.update.json` and `/releases/1.1.0.update.json` are now byte-identical to the GitHub assets. An interim publication `website-1-1-2-20260920-184659` (18:48 UTC, from `25ee10b`) served the metadata with CRLF endings because the worktree checkout converted it; it verified identically and was superseded eight minutes later; a `.gitattributes` rule (`*.update.json -text`, commit `ac0639d`) prevents a recurrence. Rollback of the current site: `python .cache/publish-website-1.1.2.py --rollback website-1-1-2-20260920-185427`.
- **Wiki** pushed as `7a4fd75` (Release-1.1.2 added; Release-1.1.1 corrected; Home, sidebar, footer, Roadmap, Getting-Started, Accounting-and-Reports, Architecture, Module-Roadmap). **About** names 1.1.2. **v1.1.1 release page** carries the dated correction block. Issues #74, #87, #88 closed; milestone 1.1.2 created (holds #88) and 1.1.1 closed; #71, #72, #73 on milestone 1.2.
- **Demo unchanged** at `core-1.1.0-af0f4895f85a` by the owner's choice; every 1.1.2 surface says so. The demo still shows the #88 behaviour and the old landing labels.
- **Upgrade proofs:** (i) 1.1.0 -> 1.1.2 by file replacement plus `install/migrate.php` on a disposable MySQL 8.4.9 schema with one company and book: exactly 034 applied, one `DEFAULT` warehouse per book, second run a no-op, preflight clean. (ii) 1.1.1 -> 1.1.2 through `/maintenance.php` with the real signed metadata: **failed** at the migrate phase with a duplicate declaration of `pl_database_platform()` between the copied recovery runtime and the application ([issue #90](https://github.com/phpledger/phpledger/issues/90)); backup and apply succeeded, the envelope was accepted, automatic recovery then restored 1.1.1 completely (1,503 files, database, sign-in page; `maintenance.php` unchanged). Present since 1.1.0; the update tests bypass the real migrate function. Every 1.1.2 surface now says: use the manual procedure until 1.1.3.
- **Record corrections:** 1.1.1 shipped migration 034 and the Stock locations module unannounced, without signed metadata, and issue #71 did not ship in it; the notes, upgrade guide, README, wiki, news page and GitHub release body are corrected, and the missing 1.1.1 receipt is backfilled (`docs/repository/PUBLICATION-2026-09-20-1.1.1.md`). The 1.1.2 package's own notes say "Published 21 September 2026" (written ahead of local midnight); the actual date is 20 September and every other surface says so; the receipt records it.
- **master:** `9a25d63` -> `06e1ca9` (#88, PR #89) -> `54fb89e` (1.2 plan) -> `3df5755` (release) -> `25ee10b` (website, wiki, receipts) -> `ac0639d` (`.gitattributes`) -> the receipt commit. **Checkout:** the shared checkout was on `fix/issue-88-pos-cash-precision` when this release started (moved there by the #88 session); the release commits were made on that branch and pushed to `origin/master` as fast-forwards. After the receipt commit the checkout is returned to `master`; the local `fix/issue-88-pos-cash-precision` branch is left as is (its remote counterpart is merged and may be deleted). New worktrees: `.claude/worktrees/release-1.1.2` (detached at `v1.1.2`, used by the signer) and `.claude/worktrees/website-1.1.2` (detached at `ac0639d`, used by the publisher); keep them until the 1.2 M1 work decides on cleanup.
- **1.2:** the owner approved decisions 1 to 4 of [RELEASE-PLAN-1.2.md](docs/strategy/RELEASE-PLAN-1.2.md) (B27 to B30). Pilot #1 starts on 1.1.2 on 1 October (findings register `docs/repository/PILOT-FINDINGS-2026-10.md`). M1 (stock-locations release surfaces, definition of done, verifier baselines) can start Monday 21 September; owner decisions 5, 5a, 6 and 12a are due by 28 September and 7 by 5 October.
- Deliberately not done: no demo cutover; no #75 build-tooling change (decision 5 open); no retro-signing of 1.1.1; no deletion of merged local branches or the older worktrees.

## Latest checkpoint, 20 September 2026 night (Claude): issue #88, POS cash tender precision

- **Branch `fix/issue-88-pos-cash-precision`, commit `06e1ca9`, pushed; pull request [#89](https://github.com/phpledger/phpledger/pull/89) open against `master` (`9a25d63`).** Issue #88 answered and left open for the owner to close on merge. The POS accepted `1262.2555` against a PKR `1,262.25` total and recorded change of `0.0055`; a tender is now held to whole minor units by `pl_cash_amount()`, the bundled catalog is refused if it prices goods finer than cash, and the review screen says why a tender is refused instead of enabling a submission the server would reject.
- The check is on the **value**, not the typed string, so a canonical `20.0000` retry still normalises identically and `request_hash` is unchanged. Storage stays `DECIMAL(20,4)`: **no migration**, no backfill, no checksum touched, and every stored row still satisfies `CHECK (change_due = cash_received - total)`.
- Evidence: `composer check` on MySQL 8.4, **319 tests, 0 failures** (316 before). Browser-driven at 1440, 768 and 390 px against a throwaway container on the isolated `phpledger_test` database. Note for anyone changing these files again: **`tools/lint.php` does not check JavaScript and `phpstan.neon` does not analyse `www/phpledger/templates`**, so `app.js` and `pos.php` have no automated cover.
- **The working tree already held another session's uncommitted 1.2 release-plan edits; they were preserved and kept out of the commit, which stages only nine files.** This checkpoint and the matching AGENT_MESSAGES entry are themselves left uncommitted for the same reason. The dev database stays at migration 031, untouched; the containers I started were stopped again.
- Deliberately not done: no merge, release, tag, media kit, wiki, website or demo publication, and no deployment. **The public demo still shows the defect.** The 1.2 release plan drafted alongside this already lists issue #88 for the proposed 1.1.2 patch.
- **Branch warning for whoever cuts 1.1.2.** The 1.2 release-plan commit `54fb89e` was committed on top of `06e1ca9` **on this pull-request branch**, not on `master`; `master` and `origin/master` are both still `9a25d63`, and `origin/fix/issue-88-pos-cash-precision` is still `06e1ca9`, so PR #89 is clean on GitHub. Version and release-note edits made from this checkout therefore land on my PR branch. Preserve `54fb89e` on its own ref before any reset, get `06e1ca9` onto `master` by merging #89 or cherry-picking it, and cut the release from `master`.

## Latest checkpoint, 20 September 2026 night (Claude): 1.2 release plan drafted; two 1.1.1 record errors found

- **Documentation only; nothing built, committed, pushed, tagged, published or deployed.** New [docs/strategy/RELEASE-PLAN-1.2.md](docs/strategy/RELEASE-PLAN-1.2.md): item-by-item code status, shape (1.1.2 patch, three signed previews, 1.2.0 stable targeted 15 January 2027, range 8 January to 12 February), milestones, publication walk, gates, fifteen dated owner decisions, risks, scope. Matching edits in `docs/ROADMAP.md` (1.1.1 line corrected, 1.2 status line), `docs/strategy/PLATFORM-ROADMAP.md` (sequence statuses), `docs/MODULE-ROADMAP.md` (stale AR allocation limit), `docs/strategy/README.md` (index row), `README.md` (stale "1.1.0 is current", 1.1.1 and 1.2 rows, line 39 correction) and `docs/wiki/Roadmap.md` (mirror). The website sources were deliberately not edited: they need a build and a publication anyway, listed for the 1.1.2 sweep.
- **Two facts every 1.1.1 surface gets wrong, verified against the tags:** (1) v1.1.1 (`63658bc`) ships migration `034_inventory_locations` and the `inventory-locations` module (absent from v1.1.0), while `RELEASE-NOTES.md:9,22`, `UPGRADE.md:33`, `README.md:39`, the wiki page, the news page and the GitHub release body say "no schema change, no migration"; a 1.1.0 operator who replaces files leaves 034 unapplied. (2) Issue #71 (demo packs out of the ZIP) did not ship: `tools/package-files.json` still lists all eleven packs and `demo_pack_functions.php` reads the literal path, although the roadmap, README, wiki, website roadmap page and platform-roadmap row 5a said 1.1.1 included it. Also: v1.1.1 has only the ZIP and `.sha256`, no signed update metadata, so 1.1.0 installations cannot in-app update to it despite `UPGRADE.md:33`.
- **Proposed next step for the owner:** decision 1 in the plan, a 1.1.2 patch by 25 September (hard date 28 September) carrying the erratum, issue #88, the first signed metadata since 1.1.0, the version-fact sweep and a website republication, so pilot #1 starts on 1 October on a true, updatable release. The erratum wording is ready in the plan. The release-notes, UPGRADE.md, wiki release page, news page and GitHub body were **not** edited here because their wording and republication are the owner's call.
- The working tree still carries the earlier session's uncommitted B25/B26 edits in five files plus the untracked `Claude outputs/`; this session added to them and did not commit. `master` remains `9a25d63`, level with `origin/master`.

## Latest checkpoint, 20 September 2026 evening (Claude): tester route and demo tool recorded as decisions

- Owner decisions **B25** (testers evaluate the published demo and published releases, not source builds or a test environment built for the purpose) and **B26** (a demo build tool is wanted and the current demo is unsatisfactory; deliberately deferred) are recorded in [DECISION-REGISTER.md](docs/strategy/DECISION-REGISTER.md) section J, [DEMO.md](docs/DEMO.md) and [ROADMAP.md](docs/ROADMAP.md). **Documentation only; nothing was built.**
- A Docker-based release test harness was explored in detail this session and **rejected by the owner**. Docker keeps its existing role (development, CI, demo deployment) and the user-facing container image stays the 1.2 roadmap item with its upgrade path in the [release protocol](docs/RELEASE-PROTOCOL.md).
- **The checkout moved twice during this session**, by other sessions: from `fix/local-install-friendly` to `master`, then to `9a25d63`. `master` is level with `origin/master`, `v1.1.1` is tagged locally and on the remote, and the 1.1.1 GitHub release was published at 16:43:25 UTC with `phpledger-1.1.1.zip` and its SHA-256. Recheck the branch before starting work in this checkout.
- Deliberately not done: no harness, no demo tool, no application code, no schema, no release, no tag, no push, no website or wiki publication, no demo change, no deployment. `docs/ROADMAP.md` is mirrored to the wiki and the public roadmap page, so those surfaces drift until the next publication.

## Latest checkpoint, 20 September 2026 (Claude): installer trigger preflight and resumable migrations

- **Merged and on `origin/master` at `65b3437`** through pull request #85, which closes issue #83. A binary-logged MySQL that does not trust the account to create triggers is named at the database connection check before any migration writes, and an interrupted migration resumes at the statement that stopped instead of needing the database recreated. Issue #83 is closed with the reply and evidence.
- CI green on the full matrix: MySQL 8.2, 8.3, 8.4 and MariaDB 10.6, 10.11, 11.4, including both browser installer fixtures. `composer check` is 309 tests, 0 failures (307 before). Verified against a disposable stock `mysql:8.4` with `log_bin_trust_function_creators=0`, which was removed afterwards.
- Migration files and their checksums are unchanged; the new `pl_schema_migrations.statements_done` column is added by `pl_migrate()`, not by a migration.
- **Note for whoever holds issue #84 (local-install friendliness):** that work was based on `ab70045`, before this merge, and touches `database_platform_functions.php` and `install_web_functions.php`, which this change also touches. Rebase onto `65b3437` first. A handover with the overlap and the design consequence is posted on issue #84. Pushing the fix branch also published the four previously unpushed stock-locations commits, and merging #85 brought them to `origin/master`; the owner accepted that.
- The website self-hosting requirements page still omits the `log_bin_trust_function_creators` prerequisite and needs a publication to carry it. `resources/release/RELEASE-NOTES.md` has no 1.1.1 section yet.


## Latest checkpoint, 20 September 2026 early morning (Claude): stock-locations module green, awaiting merge

- Branch `harvest/stock-locations` (`1bfcf37`, `85359e9`) holds the bundled `inventory-locations` module: migration `034_inventory_locations.php`, warehouse masters and transfers at carrying value, warehouse-scoped balance, history, valuation, counts, returns, AR previews and opening allocations, a Locations panel and filters on the inventory and stock-count screens, `tests/inventory_location_test.php`, packaging entries and doc wording. `composer check` passes on MySQL 8.4 and MariaDB 10.11 (307 tests, 0 failures each). Browser-checked on a throwaway server; the dev container was not migrated.
- `master` is unchanged since `993279c` (the 1.2 re-sequence documentation). The owner decides the merge; on merge the dev database (at 031) takes the ordinary upgrade path.
- Local test database container `db_test` was recreated during the MariaDB pass and restored to the default MySQL 8.4 image afterwards.
- Deliberately not done: no push, release, wiki, website, demo or deployment.

## Latest checkpoint, 19 September 2026 night (Claude): roadmap published on the wiki and website

- **Website is live at `website-1-1-0-roadmap-20260919-185525`** (published 2026-09-19 18:57:03 UTC) from branch `docs/roadmap-surfaces` commit `6e7497b`, through `.cache/publish-website-roadmap-20260919.py` over the reviewed 1.1.0 lane. Only the static root changed in the vhost; the `/support` redirects, demo proxy, headers and demo containers (`core-1.1.0-af0f4895f85a`) are unchanged. 298 files hash-verified; backup `/var/www/phpledger/data/backups/website-1-1-0-roadmap-20260919-185525`; receipt [roadmap-2026-09-19-publication.json](docs/design/website/qa/roadmap-2026-09-19-publication.json). `tools/hosting-status.py` reports the new root and no problems. Rollback: recreate the worktree (`git worktree add .claude/worktrees/roadmap-surfaces 6e7497b`) and run the wrapper with `--rollback website-1-1-0-roadmap-20260919-185525`.
- **Wiki `b1d2d16` pushed** to `phpledger.wiki` (Roadmap, Module-Roadmap, Accounting-and-Reports); `docs/wiki/` now mirrors every wiki page including `Release-1.1.0.md`. Public `/roadmap/`, `/product/` and the raw wiki pages were fetched and show the 1.1.1 / 1.2 / 1.3 sequence and the 1 October 2026 pilot.
- **Public sequence now everywhere:** 1.1.1 (demo labels, sample packages out of the ZIP) → 1.2 stock locations, trading documents, on-account receipts, plugin runtime, Users capabilities, Urdu, container image → 1.3 Arabic, table prefix, installation notice, cloud databases, remaining channels → later POS, e-commerce, API writes and directory plugins. README "Where we go from here" says the same.
- `master` fast-forwarded locally to this checkpoint; **not pushed** (owner). The main checkout remains on `harvest/stock-locations` with its uncommitted module work; that branch will need a merge from `master` (README, `docs/wiki/`, coordination files) before it lands.

## Latest checkpoint, 19 September 2026 late evening (Claude): 1.2 re-sequence documented; stock-locations module branch open

- Local `master` commits (not pushed): `d0217a4`, `ea333d7`, `2afc3df`, `9816ad9`, `40959fb`, `0418730` and the coordination update. Owner decisions B20–B23 (section H of the decision register): 1.2 = stock locations and trading-document extensions as bundled modules, core on-account receipts, plugin runtime decoupled from the table prefix, Users capabilities, Urdu/RTL, container image; table prefix, installation notice, cloud database and remaining channels to 1.3; queued entry gains a device sub-ledger with settlement posting; the owner's own company is pilot #1 from 1 October 2026.
- Work in progress: branch `harvest/stock-locations` (bundled module `inventory-locations`: migration `034_inventory_locations.php`, `pl_inventory_warehouses`, nullable `warehouse_id` on movements meaning the book default, transfers at carrying value, per-location cost, Locations panel, `tests/inventory_location_test.php`). Not merged until the full suite passes on MySQL 8.4 and one MariaDB image.
- Deliberately not done: no push, no release, no website or wiki change, no deployment. Only `Claude outputs/` remains untracked.

## Latest checkpoint, 19 September 2026 evening (Claude): housekeeping, roadmap correction, wiki, issues and discussions

- Local `master` commits (not pushed): `aa4a4bf` coordination files and `CLAUDE.md`; `f211476` Codex's two demo-landing wording lines (still not live, ships in 1.1.1); `c07ed46` the packaging decision in `docs/strategy/DECISION-REGISTER.md` (B18, B19), `docs/strategy/PLATFORM-ROADMAP.md` (new "Package directory: plugins and sample companies" section, sequence rows 5a and 5b), `docs/ROADMAP.md` (1.1.1 line, demonstration path), `docs/MODULE-ROADMAP.md` (directory packages row, items 10 and 12) and `docs/RELEASE-PROTOCOL.md` (principle 7). Only `Claude outputs/` remains untracked.
- Wiki pushed: `b2c08a9` on `phpledger.wiki` updates Roadmap, Module-Roadmap and Architecture with the package decision and brings two headers to 1.1.0. Accounting-and-Reports and First-Package still carry 1.0.0 headers; untouched.
- GitHub issues #71 to #78 (unbundle demo packs; manifest, directory and installer; plugin runtime; demo labels in 1.1.1; reproducible ZIP; account codes and hierarchy; collapsible reports; Pakistan pharmacy sample package) and discussions #79 to #82 (package RFC; first vertical plugin; reviewers and pilots wanted; account numbering design) were created at the owner's request.
- Deliberately not done: no push of `master`, so the roadmap links in the issues resolve only after the owner pushes; no website rebuild, so the public roadmap page drifts until the next website publication; no release, demo reset, deployment or test run. The owner's demo, installer and application-surface feedback is still pending.

## Latest maintenance checkpoint — 19 September 2026 (Codex)

- Local `master` was safely fast-forwarded from `75f85f7` to `97a26b4`, matching `origin/master`. The SEO changes are now represented by upstream commits; the three unique tracked instruction/coordination files and two unique untracked instruction files were preserved. Verified pre-update backup and sync receipt: `C:\phpledger-checkout-backup-20260919-153540`. No commit or push was made.
- The owner explicitly requested a private Google Drive backup of the signing folder plus a plaintext passphrase file, and selected keeping Windows Credential Manager. All 11 top-level files were uploaded and read back byte-identically. The backed-up passphrase unlocks the backed-up key; Drive permissions are owner-only. Backup details and receipt remain outside the repo in `C:\phpledger-signing-key\cloud-backup-receipt.json`. This is a cloud backup; no offline copy was created. Do not print or copy key/passphrase contents into agent messages.
- Sample-guide capacity wording is already corrected upstream in `c9743d4`. The two obsolete demo landing preview labels are corrected locally in `www/phpledger/templates/views/demo.php`, documented in `docs/DEMO.md`, and await Claude's next patch release. Sample-scenario review limitations remain intact.
- The later [1.1.0 publication receipt](docs/repository/PUBLICATION-2026-09-19-1.1.0.md) supersedes the older website/demo deployment snapshots below. This maintenance task made no live deployment or new publication claim. Real shared-host and unfamiliar-operator observation, independent accounting/security reviews, supervised pilots and cross-platform ZIP reproducibility remain open.

The earlier checkpoints below are retained as dated history.

**2026-09-19 signing-key handoff (Codex):** owner-requested RSA-4096 publisher key generated outside the repository. Public PEM: `C:\phpledger-signing-key\publisher-public.pem`; SHA-256 of DER SubjectPublicKeyInfo: `4e58a5f46b0538c9b37aaadbfced9a2d8ad2f7d413bc168a67b1b94feaa78e78`. Encrypted private key and passphrase remain local, with the passphrase in Windows Credential Manager. OpenSSL, Python and independent PHP checks passed; owner offline backup remains pending. No release archive/manifest signed, key published/pinned, or live system changed by this key-generation task. See the latest AGENT_MESSAGES entry and local KEY-CUSTODY.md. Claude retains the 1.1.0 integration/deployment lane.

Updated: **2026-09-19 13:50 UTC / 18:50 PKT** (Claude: WordPress-style install package implemented locally; see below). The earlier snapshot below, dated 2026-09-18 22:40 UTC (Codex, live SEOScore), is unchanged.

## Local work in progress

None. On the owner's instruction (19 September 2026, evening) the five merged worktrees under `.claude/worktrees/` (`distribution-protocol` 608f40e, `install-package` 9b7375d, `release-1.1.0` 97a26b4, `sample-start-gate` 0005d2b, `template-encoding` cfef7d9) were removed and their fully merged branches deleted; every commit is on `master`. The `.cache/release-0.6.0/source` and `.cache/website-redesign-live` snapshots and the `.cache/release-1.1.0/wiki` checkout are retained. The only local branch is `master`.

Pending for the next patch (1.1.1), not live: the two demo-landing wording lines in `www/phpledger/templates/views/demo.php` (committed in `f211476`, issue #74) and the owner's forthcoming demo and installer feedback.

The owner's XAMPP MariaDB (`C:\xampp\mysql\data`) has been corrupted since 1 August 2025 and does not start. The local test copy `C:\xampp\htdocs\phpledger-test` uses the `pl-xampp-db` container on 127.0.0.1:3310 until the owner confirms removal.

Read this file and the latest [AGENT_MESSAGES.MD](AGENT_MESSAGES.MD) entries before starting a task. This is the current coordination snapshot; messages retain the handoff history. Follow [AGENTS.md](AGENTS.md), [CLAUDE.md](CLAUDE.md) and the [operator runbook](docs/DEMO.md#operator-deployment-runbook-for-claude-and-codex). Recheck cheap live facts before using this snapshot for another release.

## Working together

- Before shared edits, record the agent, task, checkout/source and files or deployment surface being handled. Check the working-tree diff and latest messages to avoid conflicting with another agent's work.
- One agent owns a live deployment at a time. Coordinate the website and demo serially because their helpers can pin the same vhost baseline. Do not publish from a competing prepared plan while another agent is switching it.
- Preserve unrelated changes; do not reset the checkout, clean broadly, overwrite another agent's files, stage everything or push simply to complete a local/deployment task.
- At completion or handoff, update this snapshot and append a timestamped message with status, action, evidence and remaining work. Keep earlier messages intact. If a record is wrong, append a correction rather than silently rewriting the history.
- An explicit owner deployment request authorizes the necessary reviewed actions within that scope. Coordination does not create a new approval gate or a reason to ask for the same permission again.
- These are repository coordination files, **never website or application web artifacts**. Keep credentials, environment contents, database rows and private logs out of them.

## Current website: correct product design is live

| Item | Verified state |
|---|---|
| Public URL | `https://phpledger.com/` |
| Deployment | **Completed**, `website-1-0-0-20260918-223528`; SEOScore **97 / SEO 98 / AEO 94 / GEO 96** |
| Completion | 2026-09-18 22:37:15 UTC / 19 September 2026 03:37:15 PKT |
| Intended source | `master` base `75f85f7` plus the reviewed website-only SEO patch; source manifest in `.cache/seo-20260919/source-manifest.json` |
| Selected checkout | `C:\phpledger`, current master plus reviewed static patch. Retain `.cache/website-redesign-live` as the original design snapshot. |
| Design identity | H1: **PHP Ledger: self-hosted double-entry accounting software**; same desktop/phone captures, Pricing and Community, plus sourced summary/comparison |
| Static inventory | **295/295 files verified**; 70 HTML outputs; zero build/check errors or warnings |
| Publisher | `.cache/publish-seo-20260919.py`, frozen source/public manifest and reviewed live baseline; not a generic updater |
| Recovery backup | `/var/www/phpledger/data/backups/website-1-0-0-20260918-223528`; original/intermediate backups retained |
| Receipt | [SEO publication](docs/design/website/qa/seo-2026-09-19-publication.json) and [score evidence](docs/design/website/qa/seoscore-2026-09-19.json) |

**Source history:** the initial `record-demo-cutover` checkout had an older design also labelled 1.0.0. Claude subsequently merged the product source and deployment records; the main checkout is now `master` at `75f85f7`, with website source matching `86a1eef` before this SEO patch. Select the current reviewed source for future deployments. The older same-version site was initially published during this task; the selected master design above was then deployed and verified. Identify a requested design by its source commit, headline/screenshots and routes, not the release number alone. The old `.claude/worktrees/festive-elgamal-8a1d61` directory is empty.

The website changes the static root and the exact `/support` and `/support/` redirect pair, both `301 /pricing/`. The active vhost is `/etc/nginx/fastpanel2-sites/phpledger/phpledger.com.conf`; the older `fastpanel2-available` copy can be stale. Preserve the demo proxy, OAuth locations, TLS/ACME and existing security/noindex headers. Treat published server release directories as immutable; use a new reviewed source snapshot and release identifier for future edits.

## Demo and authentication

The demo is now **`core-1.0.0-sample-20260919-r2`**, completed at **2026-09-18T21:20:15Z**. It retains the official 1.0.0 package/source `1415ceb61cab2fe27be3947d611a16f4035e54c7` as its baseline, with a separately recorded **33-file sample-wording overlay** and unchanged runtime image. The original package manifest is preserved for provenance; use `DEPLOYMENT-OVERLAY.json` for the changed runtime hashes instead of expecting the unmodified ZIP hashes to describe every current file. The guarded sample-only reset refreshed temporary visitors and reapplied the same 32 existing migrations; no new migration or schema definition was introduced. Its database container/volume and the website/demo proxy were preserved. Fresh starter and historical sample guides plus reports passed. See [wording receipt](docs/repository/DEMO-1.0.0-SAMPLE-WORDING-2026-09-19.json). The one-shot older demo scripts require their recorded baseline and must not be replayed.

Start fresh inspection with `python tools/hosting-status.py`. The product publisher reuses `.cache/publish-website-1.0.0.py` for the saved **Windows Credential Manager** identity and pinned SSH key. No password prompt is needed. Do not request, print or reset passwords; do not bypass host-key checks. A release-baseline mismatch means the deployment plan is stale, not that the saved credentials failed. The base publisher's default static source is the older main checkout, so use the product wrapper for the design above.

## Handoff and remaining work

- **1.2 M12 built (Claude, 21 Sep 2026 06:30 UTC):** `m12/container-image` from `master` `0bb6c7d`, four commits, not pushed or merged. The production container image and its GHCR/Docker Hub publication path (B42): `docker/release/Dockerfile` (non-root, port 8080, built from a real release ZIP), entrypoint, `compose.production.yaml`, `.github/workflows/container-image.yml`, three confirmed-real installer fixes for the environment-configured-database case (`web_functions.php`, `install_web_functions.php`, entrypoint-only for the third), `pl_update_mode()` wired into `update_web_functions.php`, and a fourth bug found only by actually running the image (`AllowOverride None` 403s on this Apache build; fixed with `AllowOverride FileInfo` in `docker/release/apache.conf`). `composer check` 457 tests 0 failures; new `tests/container-image-test.py`/`container-upgrade-test.py`/`container-tags-test.py` all green against a real built image. See the message of the same time for full evidence and two flagged-but-not-fixed out-of-scope issues (a stale `.dockerignore` pattern; a pre-existing unrelated failure in `browser_installer_keyless_test.php`). No migration, no accounting/posting file, no `public/maintenance.php` byte change, `VERSION` not bumped.

- **Issue #90 fixed on a branch (Claude, 20 Sep 2026 20:35 UTC):** `fix/issue-90-updater-migrate` (`273de78` plus this record), not pushed or merged; VERSION 1.1.3 and the 1.1.3 release surfaces are on it. The in-app updater completes again: new `tests/update-migrate-test.php` and a 1.1.1 -> locally built 1.1.3 proof through `/maintenance.php` with a test key both reach `complete`. See the message of the same time. Owner items: review and merge, official signing, publication, README/wiki/website, receipt.

- **1.1.0 is live (Claude, 19 Sep 2026 15:35 UTC):** release v1.1.0 (tag `af0f489`, signed metadata, key fingerprint `4e58a5f4…aa78e78`), demo `core-1.1.0-af0f4895f85a`, website `website-1-1-0-20260919-152351`, origin/master `97a26b4`. The sections above describing the 1.0.0 website and demo are now historical; see `docs/repository/PUBLICATION-2026-09-19-1.1.0.md` and the latest message. The main checkout is behind origin/master and still holds the SEO patch as uncommitted edits identical to the committed files.

- **1.2 M4 on a branch (Claude, 21 Sep 2026):** `m4/stock-documents` from `master` `3c0a5ae`, one commit, not pushed or merged. Warehouse kinds (a van is a `mobile` location with a driver), a numbered stock-document family over the existing movements (stock issue, re-issue, return from van, gate pass) on the M2 series per kind, non-posting gate passes, the van settlement review with an approval seam for the Users module, stock-by-location / van-stock / aggregate reports with the manifest's `stock-by-location` wired to `/reports/stock-by-location`, three print templates through the M2 registry, and API/MCP reads of warehouses and transfers. Migration placeholder **037**, renumber at merge (B62). The `inventory-locations` manifest moves to 1.1.0, so its digest changes and an installed company sees the ordinary module upgrade prompt. `composer check` green on MySQL 8.4: 373 tests, 0 failures; `tests/package-builder-test.py` 13 tests OK. Runs in parallel with M3 and M5 and touches neither `ar_ap_functions.php` nor `settlement_functions.php`/`open_item_functions.php`; it does not rebuild `public/assets/app.css`. A second commit adds `tests/stock-http-smoke.py` (81 HTTP checks, 0 failures) closing definition-of-done item 2 for this module, and — **for the attention of M2 and M6** — adds `numbering` and `owner` to `pl_render()`'s template allowlist: both were added to their routes but never to that allowlist, so `/numbering` and `/owner` answer **503 on `master` today**. See the messages of the same date.

- **1.2 M6 on a branch (Claude, 21 Sep 2026):** `m6/chart-codes-contra-owner` from `master` `86e67d1`, one commit, not pushed or merged. Structured account codes `X-XXX-XXXXX-XX` with the conversion migration (placeholder number **036**, renumber at merge) and an immutable old-to-new mapping; contra accounts presented as deductions inside their own section; owner capital, loans, repayments and drawings over the core posting service with partner capital accounts and profit-sharing ratios; collapsible trial balance, profit and loss and balance sheet. `composer check` green on MySQL 8.4: 335 tests, 0 failures. It rebuilds the generated `public/assets/app.css` from `ext/reports.css`, so whichever of this and M2's print-pipeline branch merges second re-runs `npm run build:css`. Profit allocation between AOP partners is left for the accountant (B30). See the message of the same date.

- **1.2 M5 on a branch (Claude, 21 Sep 2026):** `m5/advances-refunds` from `master` `3c0a5ae`, one commit, not pushed or merged. On-account receipts with an oldest-first plan that stays editable, unapplied credit held as a **customer-advances liability control** tracked as advance open items (B59 — not a credit balance inside receivables and not a suspense account), later application with no bank line, cash refunds, credit notes with no original invoice, the supplier mirror, and batch receipts posting one voucher per party row (B57). Migration **`037_advances_and_refunds` (placeholder number, renumber at merge)**; new `advance_functions.php`; new `tests/advance_test.php`. `composer check` green on MySQL 8.4: 379 tests, 0 failures; `tests/package-builder-test.py` OK. It makes three small, deliberate edits inside `ar_ap_functions.php` (open-item entry-kind arithmetic and excluding advance items from the receivables/payables ageing) that the M3 branch must keep. The AR-document form of an orphan credit note and the statement's Unapplied rows are left for M3, which should call `pl_unapplied_credit()`. The representation goes to the accountant with worked examples (B30). See the message of the same date.

- **Distribution protocol and platform roadmap (Claude, 19 Sep 2026):** merged and released in 1.1.0; `docs/RELEASE-PROTOCOL.md` and `docs/strategy/PLATFORM-ROADMAP.md` are on `master`. The earlier note that master could not be packaged is obsolete.

- **Owner scope correction, 21:18 UTC:** Claude is the requested owner for the complete account-numbering, collapsible-report and Pakistan pharmacy sample-company feature work. Codex owns only website/demo deployment and the deployment handoff. This is a requested file-based handoff, not confirmation that Claude has started.
- **Feature status: pending; no feature implementation was made or deployed by Codex.** Scope for Claude: padded `X-XXX-XXXXX-XX` account codes and conversion of existing account numbers using their current groups; named parent/subaccount hierarchy such as `1-000-00000-00 Assets` → `1-001-00000-00 Cash` → `1-001-00002-00 Petty Cash` → `1-001-00002-01 Qasim` / `1-001-00002-02 Office`; collapsible Trial balance, Profit & loss and Balance sheet; and a Pakistan pharmacy sample company using an Odoo chart-of-accounts reference with fictional data. Claude must inspect the latest messages and actual WIP before continuing. Do not include unfinished feature changes in deployment.
- **Feature stop receipt:** both feature subtasks stopped before implementation. The account/report helpers, report templates and partials, `public/assets/app.js`, `public/assets/app.css` and `resources/ui/ext/reports.css` have no scoped changes; no feature patch, CSS rebuild or feature test was produced. Research and proposed interfaces are recorded in the messages. The owner explicitly requested no full retest.
- Codex completed the live website correction, demo wording deployment, repository receipts and owner handoff. No concurrent live deployment is needed. Claude acknowledged feature ownership in the later messages; accounting/report/pharmacy work remains outside this website task.
- The earlier **320 px header overflow is resolved** in the SEO patch. Focused homepage checks now pass at **1440, 768, 390 and 320 px**, including the mobile menu, comparison-table keyboard scroll and contents anchors. Earlier ten-route evidence remains historical; no full retest ran.
- Browser evidence: `output/playwright/deploy-1.0.0-20260919/product-live-smoke-result.json` and `product-live-home-1440.png`. These local output files are not release assets.
- No new migration or schema definition was introduced. The demo wording cutover reused the runtime image and reset temporary sample data using the same 32 existing migrations. No Git push was made and no credentials were exposed. Follow the live receipts and final handoff for later verification updates.

## Repository cleanup for the fresh start — 2026-09-18 22:25 UTC (Claude)

- Pull requests #68 (org move), #69 (sample wording) and #70 (demo cutover record) are merged. No pull request is open.
- Remote branches: only `master` and the CLA bot's `cla-signatures` remain. Merged branches were deleted. The unmerged experimental branches were archived as GitHub tags before deletion: `archive/Branch-Test-2`, `archive/fix-jv-mansoor`, `archive/new-phpledger`.
- Local branches: only `master`. Codex's unmerged local branches were archived as tags before deletion: `archive/cash-account-integrity` (includes a WIP snapshot commit with `docs/repository/sprint-06/CASH-ACCOUNT-INTEGRITY.md`), `archive/owner-questions-ar-ap`, `archive/quotes-plugin`, `archive/regional-catalog-ux-review`. The owner pushes them with `git push origin --tags`.
- The work Codex had stashed before the fresh start (sample wording in demo packs, sample data, tests and docs; the receipts; the runbook; `tools/hosting-status.py`; these coordination files) is committed on `master` so the repository matches what is live. Nothing was dropped.
- Worktrees retained as Codex's pinned deployment snapshots: `.cache/release-0.6.0/source` and `.cache/website-redesign-live`. The `.cache/ux-integrity-fix` worktree was removed after its note was committed to its archived branch.
- Left for the owner to review and commit: `AGENTS.md` (adds the coordination-file rule) and `CLAUDE.md` (new, written by Codex); the app does not let Claude commit its own instruction files. `master` is ahead of `origin/master`; the owner runs `git push origin master --tags`.
- Next work (Claude, fresh session on 19 September 2026): the account-numbering, collapsible-report and Pakistan pharmacy sample features recorded in the handoff above. Codex keeps deployments.

## Scope claim, 21 September 2026 (Claude): 1.2.1 M9, the counter POS and the distribution behaviour

- **Branch `m9/pos-and-distribution`, rebased onto `master` `3fc315d` (it started from `cdd9dba`, tag `v1.2.0`), worktree `.claude/worktrees/agent-ad1f6c2ce3475be05`.** Local only: no merge, no push, no publication.
- **Owned paths.** New: `www/phpledger/includes/functions/counter_pos_functions.php`, `counter_pos_web_functions.php`, `distribution_functions.php`; `www/phpledger/templates/views/counter.php`; `tests/counter_pos_test.php`, `tests/distribution_simulation_test.php`; `tools/simulate-distribution.php`. Edited: `www/phpledger/public/index.php` (routes), `web_functions.php` (`pl_render()` allowlist, navigation), `capability_functions.php` (two cost-visibility report ids), `stock_document_functions.php` (the van settlement gains the sales side, the credit-limit check and cost masking), `tests/i18n_test.php`, `tests/run.php`, `tools/package-files.json`, `resources/modules/inventory-locations.json`, docs.
- **Explicitly not touched:** `resources/demo-packs/` and `demo_pack_functions.php` — the distributor van day in the packs belongs to `m13/sample-data-refresh`. The offline device queue stays on its own track. **No migration is used at all**: a counter sale is an invoice, a van sale is an invoice, the credit limit is the column migration 014 already carries and the cost split is a read, so M9 consumes no number. (The claim originally said 042 would stay free; by the rebase 042, 043 and 044 had gone to the plugin runtime, the sample skeletons and the ownership register, and M9 still took none of them.)

## Codex active ownership — 23 September 2026: M17 secret store

Local takeover in agent-a2c82cf1a04045756: migration 049, authenticated private-key storage, plugin secret API, tests and packaging. Claude paused; coordinator owns combined integration. B83 governs the key lifecycle; no connector/provider or production scope.

- **M17 secret-store takeover completed locally (Codex, 23 Sep 2026):** `m17/secret-store`, migration 049; secret API 1.1.0, scope-authenticated encryption, fail-closed key lifecycle, recoverable rotation and matched updater key backup. 123 isolated MySQL plugin tests and updater recovery passed; 11 PHP lints clean. Coordinator owns integration and combined gates. No publication, production mutation or connector/provider call.
## 2026-09-23 — Codex employee-master lane handoff

`m17/employee-master` in `agent-aae80d50108203030` completes the preserved employee WIP locally: migration 048, restricted register, dated status reads, UI, package registration, docs and nine passing service/HTTP tests. Lint 349/0, PHPStan clean, sample checks pass and package-builder 13/0. Full lane suite exceeded the default Composer 300s timeout; parent runs combined engine/package/browser gates and integrates employee hook declarations/API snapshot. No push/deploy/live changes. See the appended detailed employee handoff in AGENT_MESSAGES.MD; older shared-state entries remain historical.
## 23 September 2026 — M17 period-close takeover handoff

Codex completed the paused period-close/reversing-journal/cash-count lane locally on actual branch `worktree-agent-a473613d91ab7aa9d` (the older claim named `m17/period-close`). Migration 047 remains reserved. Targeted PHP 8.3/MySQL 8.4 suite: **36 tests, zero failures**, including HTTP write controls and concurrency/rollback evidence; changed PHP lint and diff check clean. Coordinator integrates the local commit and owns combined engine/package/browser gates and plugin API snapshot. No merge, push, release or live change from this lane. See the latest agent message and `docs/design/1.2-2026-09/PERIOD-CLOSE.md`.
