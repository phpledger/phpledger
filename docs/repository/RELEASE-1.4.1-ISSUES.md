# 1.4.1 issue acceptance audit

Local source review on 24 September 2026, on the isolated `codex/release-1.4.1` branch based on `f325d02d`. This is an audit of issue bodies and current source, not a claim that 1.4.1 is published or installed. No issue was closed or commented on by this audit.

| Issue | Current evidence | Acceptance still to establish |
|---|---|---|
| [#71 — demo packs outside core ZIP](https://github.com/phpledger/phpledger/issues/71) | `pl_sample_package_directory()` defaults to private `storage/packages/samples` with a configurable override; repository fallback is limited to local/test/demo. `demo_pack_functions.php` uses the resolver, `setup_functions.php` retains the neutral `core-accounting-1.0.0.json`, `tools/package-files.json` omits demo packs, and the package-builder test excludes `resources/demo-packs/`. `compose.demo.yaml` mounts a signed preload directory read-only. `templates/views/onboarding.php` links to `/packages` and the public directory. The older issue wording about mounting repository packs has been superseded by signed sample packages. | Confirm the exact 1.4.1 ZIP omits all eleven packs while retaining the neutral starter; run package-builder/demo pack/demo smoke and `tools/verify-demo.ps1` where applicable; prove all eleven signed preloads seed on the local demo and browser-check the chooser link. Keep publication and live demo proof separate. |
| [#100 — panel upgrade entry point](https://github.com/phpledger/phpledger/issues/100) | `www/phpledger/install/upgrade.php` exists, is CLI-only, runs preflight and migration verification, reports already-current or failure, and is listed in `tools/package-files.json`. `tests/installer_test.php` covers current-install idempotence and sanitized failure; `docs/RELEASE-PROTOCOL.md` now names the real path. | Verify a disposable prior-release installation upgraded from the exact candidate package reaches current schema, and verify the shipped archive contains the entry point. The panel's backup/extract/invoke workflow and rollback remain #101 work. Issue closure waits for release proof. |
| [#102 — exposed operational credentials](https://github.com/phpledger/phpledger/issues/102) | `docs/CREDENTIALS.md` now gives provider-specific revocation routes, replacement, secure secret installation, artifact/access review and a private record. `SECURITY.md` links it. The existing B86 `gh secret set` prevention rule and publisher-key rotation guide are referenced. | Review the new instructions against current provider consoles when an actual incident occurs. This documentation change does not revoke any existing token or establish that a past exposure was contained. Issue closure waits for the published documentation. |
| [#104 — receipt/expense heading](https://github.com/phpledger/phpledger/issues/104) | The current server render chooses receipt or expense from `kind` in `public/index.php` and `templates/views/editor.php`, but the issue concerns switching transaction type after the page loads. The 1.4.1 coordinator owns the screen/interaction change. | Browser-check both switch directions and direct Home quick actions at desktop and phone widths, including heading and category coherence. Test the exact packaged build and confirm published behavior before closing. |

The [#92–#98 issue group](https://github.com/phpledger/phpledger/issues/92) needs independent substantive review before closure or claims of full acceptance. That includes ownership and share-ledger controls (#92), period close and reversing journals (#93), recurring/scheduler behavior (#94), fixed assets (#95), loan schedules (#96), year-end (#97), and payroll (#98). Existing implementation and local test receipts are useful technical evidence, but an accountant must review the financial rules and worked cases, and security/access and upgrade paths need separate review. Do not infer assurance from the 1.4.0 release or its tests.

[#72](https://github.com/phpledger/phpledger/issues/72), [#75](https://github.com/phpledger/phpledger/issues/75), [#78](https://github.com/phpledger/phpledger/issues/78), [#99](https://github.com/phpledger/phpledger/issues/99), [#101](https://github.com/phpledger/phpledger/issues/101), and [#103](https://github.com/phpledger/phpledger/issues/103) remain separate deferred work. In particular, #103's MySQL trust-function acceptance cannot be established by adding `DETERMINISTIC` declarations mechanically; routine bodies that read or write data need truthful SQL characteristics and a real binary-logging installation test. The 1.4.1 patch does not claim those issues complete.

Before changing any issue status, compare the final exact 1.4.1 archive and published release with this register, run the relevant acceptance workflows, and link a dated receipt. A source-level fix alone does not establish that users can obtain it.
