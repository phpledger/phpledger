# PHP Ledger 1.4.6 publication receipt — 26 September 2026

Owner instruction of 26 September 2026: fix the sample gallery, the statements and the partner capital, and redeploy the demo ("when I wake up, I want to see all this fixed and demo worth viewing"). This receipt follows [`docs/RELEASE-PROTOCOL.md`](../RELEASE-PROTOCOL.md).

| Step | Status | Evidence |
|---|---|---|
| 1. Freeze and version | done | Branch `claude/setup-1.4.5`: corrections commit `334cc78f` (B98 to B100), gate receipts `b8659eb0`. `www/phpledger/VERSION` is `1.4.6`. Local gates: [RELEASE-1.4.6-GATES.md](RELEASE-1.4.6-GATES.md). |
| 2. Tag | done | Annotated tag `v1.4.6` at `334cc78f`, pushed to `origin` on 26 September 2026 07:20 UTC (tag object 02b5e3d2). |
| 3. Build in CI | done | `release.yml` run 36226456409 succeeded: build, byte-identical rebuild, draft release with `phpledger-1.4.6.zip` (SHA-256 `a39e7ddb252265382719390bd78e124306c2195e052fb722dcf8baffa529664c`). |
| 4. Reproduce locally | done | `tools/build-release.py --compare <CI asset>`: `reproduction: members (same member set and per-member SHA-256; the CI zip compresses differently)`. The gated local archive is `.cache/release-1.4.6/phpledger-1.4.6.zip`, 3,643,443 bytes, SHA-256 `dadc2758d9c50fff109e79f8448c4cb8010d4a9d51a458ce190d4512cf7765f1`, 1729 members, built from `334cc78f8582982fb1ff3e400d1b2deab925bc22`. |
| 5. Sign offline | done | `.cache/release-1.4.6/sign-1.4.6.py` (pattern of 1.4.5): passphrase from Windows Credential Manager in-process, `tools/sign-update.php` from the `v1.4.6` worktree in a network-less container; `phpledger-1.4.6.update.json` SHA-256 `02ac42be37f54a02c035666fb092120542c3a50aa00a58b55ef6b1b62fc14d9e`, verified as an upgrade from 1.1.0, 1.1.1, 1.1.2, 1.1.3, 1.2.0, 1.2.1, 1.3.0, 1.4.0, 1.4.1, 1.4.5 and rejected for 1.4.6. |
| 6. Media kit | skipped | Patch release. |
| 7. Publish the release | done | Gated archive, sidecar and signed metadata uploaded over the draft assets; published as latest at https://github.com/phpledger/phpledger/releases/tag/v1.4.6 (published 26 September 2026 07:22:51 UTC). Anonymous download: archive SHA-256 `dadc2758d9c50fff109e79f8448c4cb8010d4a9d51a458ce190d4512cf7765f1`, metadata SHA-256 `02ac42be37f54a02c035666fb092120542c3a50aa00a58b55ef6b1b62fc14d9e`. |
| 8. Feed | done and live | Commit `62ad326c`: 1.4.6 at the top of `www/website/src/releases.json` (archive digest, `update_json` at `https://phpledger.com/releases/1.4.6.update.json`, image `sha256:04665423f4865ba4b03c93bb98b101f9be7a02624ab4d34c53e9c2d16ad3910f`), `site.json` release block, the signed metadata mirrored byte-for-byte at `src/static/releases/1.4.6.update.json`, the public output rebuilt (102 pages, 0 errors, 0 warnings). Published to phpledger.com through the reviewed operator lane on 26 September 2026 (see Deployment); `verify_public.py` fetched all 417 public files and the two routing exceptions with matching hashes, the feed's stable channel reports 1.4.6 with the archive digest and image index, the signed metadata verified with the publisher key, the release ZIP downloaded from GitHub matched, and the eleven sample envelopes verified. |
| 9. Container image | done | Image index `sha256:04665423f4865ba4b03c93bb98b101f9be7a02624ab4d34c53e9c2d16ad3910f` on GHCR and Docker Hub (container-image run 36226600772 (first index sha256:bf627586…, superseded the same hour), docker-hub-push run 36226887434; `verify_registry.py` copied the application root out of both registries' linux/amd64 and linux/arm64 images without starting a container and found every one of the 1,728 managed files plus the manifest byte-identical to the gated ZIP; both registries resolve the same index); tags 1.4.6, 1.4, 1, latest; pulled image reports 1.4.6 from `334cc78f8582982fb1ff3e400d1b2deab925bc22`. |
| 10. Registries and catalogues | done | Packagist lists v1.4.6 (repo.packagist.org/p2/phpledger/phpledger.json lists v1.4.6). Catalogue manifests moved to 1.4.6 in `62ad326c (feed, site metadata, mirrored signed metadata, public output, catalogues, container defaults) and ac6bbf51 (remaining 1.4.6 default pins)`; no catalogue request opened. |
| 11. Upgrade proof | done | Packaged `install/upgrade.php` on a populated 1.4.5 seed: no-op twice on MySQL and MariaDB with rows and balances unchanged (exact-package gates). Signed metadata: accepted by `pl_update_verify_metadata()` as an upgrade from 1.1.0 through 1.4.5, refused for 1.4.6. In-app update through the public feed: from the extracted published 1.4.5 package, `pl_release_feed_parse()` and `pl_release_feed_available()` read the live feed and offered 1.4.6, and the live `1.4.6.update.json` verified with the pinned publisher key as an upgrade from 1.4.5 (archive `dadc2758…`, 3,643,443 bytes, 1,729 inventory files); receipt `.cache/release-1.4.6/upgrade-proof.json`. Container tag pull: the pulled images report 1.4.6 from `334cc78f` (registry verification). |
| 12. Receipt | done | This file and [PUBLICATION-2026-09-26-1.4.6.json](PUBLICATION-2026-09-26-1.4.6.json). |

## Deployment

Attempt `release-1-4-6-20260927-r1` through the reviewed finite operator lane (`.cache/release-1.4.6-operator`, adapted from the 1.4.5 and 1.4.1 lanes; 45 local guard tests and every self-test passed): read-only host baseline captured at 07:37 UTC and reproduced by the adapted operator (15 containers, 36 configuration files, port 18206 and subnet 10.204.87.0/24 vacant); staging set built from the registry receipt, the gated ZIP, the gate receipt, the committed website tree at `ac6bbf51` and the 44 signed v1.1.0 preloads (validated locally, then reserved, uploaded and extracted on the host: 9 hosting, 44 sample and 417 website files); preflight, prepare (image pulled and its manifest checked, private backups of the vhost, baseline files and environment), launch (new Compose project `phpledger-demo146` on loopback 18206, disposable database initialised and reset, readiness on loopback), loopback probe (health installer-ready, install redirect, four-stage installer with the shared-demo notice, CSRF and secure cookie flags; no form submitted) and cutover (atomic vhost swap: static root to `release-1-4-6-20260927-r1-website`, three demo/discovery upstreams 18205 to 18206, notice route unchanged). Post-cutover `verify_runtime.py`: both health URLs 200 installer-ready, database healthy, four new containers without restarts, 44 preload hashes, the running package manifest `fdd51425…` for 1.4.6 from `334cc78f`, all fifteen prior container identities and 36 configuration files unchanged, Nginx changed only in the root and the three upstreams, scheduler script pinned, reset marker valid until 08:00 UTC. The 1.4.1, 1.4.0 and 1.3 demo generations and the notice services keep running untouched; the never-launched 1.4.5 staging directories remain on the host. Scheduled-reset observation across the 08:00 UTC boundary (`reset-observation-r1.json`): the demo's generation changed at 08:00:00 UTC with no manual reset, the four container identities stayed the same with zero restarts, and both health URLs were back at installer-ready.

## Evidence the owner asked for

Owner asked for evidence of the signed sample catalogue and the one-click install. (1) `.cache/release-1.4.6/evidence/directory-signature-evidence.json`: the live `https://phpledger.com/directory/index.json` lists 11 packages and every `sample-envelope.json` verified with the publisher key (SPKI `4e58a5f4…`) and binds its `package.json` by digest. (2) On a fresh production-mode copy of the 1.4.6 code (installer completed against an isolated database, no repository fallback), the Start stage showed all eleven companies from the bundled catalogue with logo, story, kind and counts, and one click on Willow Corner Shop's Install button fetched the directory and the signed envelope from phpledger.com and the archive from GitHub, verified them with the key pinned at install time (`storage/installation/publisher.pem`, SPKI `4e58a5f4…`) and installed the sample; the card then read Installed. (3) A partnership set up with two partners at 50 percent each and 50,000 SGD capital introduced shows `Partner capital - Rana Mansoor` 25,000.00 and `Partner capital - Sara Khan` 25,000.00 under Capital and Reserves, total equity 50,000.00; the balance sheet, trial balance and profit and loss print no amount on an open heading and one total per group, with zero lines hidden. Screenshots at 1366×768: `.cache/release-1.4.6/evidence/*.png` (Start gallery, balance sheet and its equity section, trial balance, profit and loss, Packages Directory and Sample companies tabs, Home, the live public demo installer and the live download page).
