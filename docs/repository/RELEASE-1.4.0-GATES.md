# PHP Ledger 1.4.0 exact-artifact gates — 23 September 2026

This records technical release gates for immutable source `8fface957e1bac2a7292b8125dc57170c24b6ebf`, compared with published 1.3.0 documentation baseline `170949d4177513c3658d82bc301de1a63a7aad20`. It is **not the hosted-production publication receipt**. The companion JSON lists every one of the 279 changed source paths in that Git comparison. Website finalization and live cutover need their own source, hash and public acceptance records.

## Frozen source and application artifact

- Final local `phpledger-1.4.0.zip`: 3,566,753 bytes, SHA-256 `53c7fc584bd47723a6f6008a8a951db9b3947dae956484c8bf3b889ce2fad4d2`; 1,695 archive members (1,694 managed payload files plus `PACKAGE-MANIFEST.json`). The signed update envelope's SHA-256 is `b511aadbe358a80195d142aec47ddeca180601d9b1c971f7225c811d919a0931`; its publisher SPKI pin is recorded in `.cache/release-1.4.0-publish/signing-receipt.json`. The three exact public assets were anonymously downloaded and hash checked per `public-downloads.json`.
- `runtime-equivalence.json` compares this source with `3f8a34addea5491b3847ce433d67e8a8df204354`: every runtime payload member is identical; only the source-bearing package manifest changed. The local reproduction passed. Release workflow [35894400785](https://github.com/phpledger/phpledger/actions/runs/35894400785) produced a ZIP with different compression but the same 1,695 member names and per-member bytes (`ci-reproduction.json`).

## Application and upgrade evidence

- Frozen RC2 `composer check` logs `.cache/full-mysql-rc2.log` and `.cache/full-maria-rc2.log`: **694 tests, zero failures on each** MySQL 8.4 and MariaDB 10.11; 415 PHP files lint clean and PHPStan reported no errors. The focused policy run passed **57/57** (`.cache/cash-policy-worker-focused.log`). The final CI runs [35892925771](https://github.com/phpledger/phpledger/actions/runs/35892925771) and [35893694707](https://github.com/phpledger/phpledger/actions/runs/35893694707) each passed all nine jobs. Initial merged CI on `877d10` had five redirect, help bounds, missing-help and namespace failures; initial local full runs each had 32 failures from mutable checksums and shared test storage. Integration fixes and isolated frozen RC2 reruns resolved those preliminary blockers; the failing logs are not counted as release passes.
- Exact ZIP fresh installs passed **61 migrations** on both engines (`.cache/artifact-fresh-{mysql,maria}-publish.log`). They exercised party-linked receipts, cash refusal, the exact bank facility boundary, reports and migration no-op replay. Migrations **057–060** follow the published chain through 056; existing books default to warning-only, and strict mode is an explicit administrator choice.
- The official signed HTTP upgrade from published 1.3.0 completed its backup, apply, migrate, verify and runtime phases, preserving historical accounts, periods, journals, partial invoice payment and balances (`.cache/artifact-http-publish.log`). Fault injection after migrations restored the exact 1.3.0 matched state (`.cache/artifact-recovery-publish.log`). A disposable container upgrade from the published 1.3.0 image to the exact 1.4.0 production image preserved the populated baseline (`.cache/artifact-container-upgrade-publish.log`).
- Local browser acceptance covered warning → strict → warning policy saves, audited reasons and keyboard submission at 1440, 768 and 390 px, with no console errors (`.cache/policy-browser-acceptance.md`). Receipt/expense party and contextual-guidance checks passed **16/16** on each database engine (`.cache/party-guidance-{check,maria-check}.log`).

The owner waived this delivery's media kit. These checks establish the tested application and update behavior; they do not establish independent accounting/security assurance, native-language review, real-business pilots or provider acceptance. The website, shared demo, registry digests, Wiki/About review and live host state belong in the separate publication receipt and must be recorded from their actual final deployments.
